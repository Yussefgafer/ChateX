# 👻 ChateX (GhostMesh) - الدليل التقني الشامل (The Complete Technical Manifesto)

مرحباً بك في مشروع **ChateX**، الحل الأمثل للمحادثات اللامركزية المشفرة. هذا الملف هو المرجع النهائي لفهم كل سطر كود، كل قرار معماري، وكيفية بناء وتوسيع هذا النظام المعقد.

---

## 🌟 الرؤية والأهداف (Vision & Goals)

**ChateX** ليس مجرد تطبيق شات؛ هو نظام بيئي للتواصل في الأماكن المنعزلة أو حالات الطوارئ. 
- **Decentralization:** لا توجد خوادم مركزية. الأجهزة هي الخوادم.
- **Privacy First:** تشفير AES-256 لكل "Packet" يمر في الشبكة.
- **Material 3 Expressive:** تجربة مستخدم "حية" (Fluid) تتفاعل مع حالة الشبكة.

---

## 🏗️ الهندسة المعمارية للنظام (System Architecture)

يعتمد التطبيق على معمارية **MVVM + Repository Pattern** مع طبقة خدمات (Background Services) لضمان استمرارية الاتصال.

### 1. طبقة الـ Mesh Core (The Heart)
تقع في حزمة `com.kai.ghostmesh.mesh`:
- **MeshManager:** المنسق الأساسي لـ Google Nearby Connections. يقوم بإدارة الـ Discovery والـ Advertising والـ Connection Lifecycle.
- **MeshEngine:** العقل المدبر لتوجيه البيانات. يحتوي على منطق الـ **Multi-hop Relay**؛ إذا استقبل الجهاز رسالة ليست له، يقوم بإعادة بثها (Relay) مع تقليل الـ `hopCount`.

### 2. طبقة البيانات (The Memory)
تقع في `com.kai.ghostmesh.data`:
- **Room Database:** تخزين الرسائل والبروفايلات محلياً.
- **GhostRepository:** طبقة التجريد (Abstraction) التي تجمع بين بيانات الـ Mesh والبيانات المخزنة محلياً. تدعم ميزة الـ `burnExpired` لمسح الرسائل ذاتية التدمير.

### 3. الخدمات الخلفية (The Ghost Service)
- **MeshService:** خدمة تعمل في الخلفية (Foreground Service) تبقي الاتصال حياً حتى لو أغلق المستخدم الواجهة. تقوم بإظهار الإشعارات ومعالجة الرسائل الواردة فوراً.

### 4. التشفير والأمن (Spectral Security)
- **SecurityManager:** يستخدم `AES/CBC/PKCS5Padding` لتشفير الـ Payloads قبل إرسالها. 
- **Self-Destruct (Burn After Reading):** يتم تحديد وقت انتهاء الصلاحية في الـ Packet، ويقوم الـ `GhostRepository` بمسحها آلياً عند انتهاء الوقت.

---

## 📡 بروتوكول التواصل (Spectral Packet Protocol)

كل تواصل يتم عبر كائن `Packet`:
```kotlin
data class Packet(
    val id: String,          // UUID فريد للرسالة
    val senderId: String,    // معرف المرسل
    val receiverId: String,  // معرف المستقبل (أو "ALL" للبث العام)
    val type: PacketType,    // نوع الرسالة (CHAT, IMAGE, VOICE, PROFILE_SYNC, ACK)
    val payload: String,     // المحتوى المشفر (Base64)
    val hopCount: Int,       // عدد القفزات المتبقية (للتوجيه)
    val isSelfDestruct: Boolean // هل الرسالة للتدمير الذاتي؟
)
```

---

## 🎨 واجهة المستخدم التعبيرية (Expressive UI)

ChateX يستخدم أحدث إصدارات **Material 3 Expressive**.
- **Radar Screen:** واجهة تفاعلية تعرض الأجهزة القريبة كـ "أرواح" (Ghosts) تظهر وتختفي بناءً على حالة الاتصال.
- **Morphing Icons:** استخدام `graphics-shapes` لتحويل الأيقونات (مثلاً تحول أيقونة القفل عند التشفير).
- **Wavy Progress:** تموجات بصرية في مؤشر التحميل تعبر عن "ذبذبات" الشبكة.
- **Spectral Colors:** باليت ألوان داكنة (PhantomBlack, EctoplasmGreen, SpiritPurple).

---

## 📂 خريطة الملفات (Project Structure Map)

- `MainActivity.kt`: نقطة الدخول، إدارة الصلاحيات (Permissions)، والتنقل (Navigation).
- `ui/GhostViewModel.kt`: المنسق بين الـ UI والـ Repository والـ Service.
- `ui/components/`: المكونات البصرية المخصصة (MeshRadar, MorphingIcon, SpectralGlow).
- `mesh/MeshEngine.kt`: منطق الـ Relay وتدفق الـ Packets.
- `security/SecurityManager.kt`: منطق التشفير وفك التشفير.
- `model/Models.kt`: تعريفات البيانات (Packet, Message, UserProfile).

---

## 🛠️ دليل المطور (Developer's Guide)

### البدء السريع:
1. تأكد من استخدام **JDK 17** و **Android Studio Ladybug**.
2. فعل **Bluetooth** و **WiFi** و **Location** (المتطلبات الأساسية لـ Nearby API).
3. لبناء نسخة تصحيحية: `./gradlew assembleDebug`.

### الصلاحيات المطلوبة (Permissions):
يحتاج التطبيق لصلاحيات حساسة للعمل على Android 13+:
- `NEARBY_WIFI_DEVICES` (بدون الحاجة لموقع في الإصدارات الأحدث).
- `BLUETOOTH_SCAN/ADVERTISE/CONNECT`.
- `ACCESS_FINE_LOCATION` (للتوافق مع الإصدارات القديمة).

---

## 📝 سجل تحديثات الفهم (Agent Knowledge Log)

- **[26 فبراير 2026]:** تم عمل مسح شامل للكود. اكتشفنا أن الـ `MeshManager` يدعم الـ **Stealth Mode** (الاختفاء من الرادار مع البقاء قادراً على الاستقبال).
- **ملاحظة:** نظام الـ Relay يعتمد على `hopCount` افتراضي (3 قفزات)، مما يعني نظرياً إمكانية التواصل حتى مسافة 300-400 متر عبر الأجهزة الوسيطة.

---
*تم إعداد هذا الملف بواسطة Kai-Agent - شريكك التقني المخلص.*
